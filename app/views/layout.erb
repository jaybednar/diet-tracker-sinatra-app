<!DOCTYPE html>

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

  <title>DietTrackerSinatraApp</title>

  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  <link rel="stylesheet" href="/stylesheets/main.css" />
</head>

<body>
  <div class="wrapper">
  <nav>
      <% if logged_in? %>
        <a href="/users/<%=current_user.id%>"><button> My Account </button></a>
        <a href="/logout"><button> Logout </button></a>
  
        <p>Welcome, <%=current_user.username %></p>

      <% else %>
        <a href="/users/new"><button> Sign Up </button></a>
        <a href="/login"><button> Sign In </button> </a>
      <%end %>

    </nav>
  
    <% if !@error_message.nil? %>
      <div class="alert alert-warning">
        <%= @error_message %>
      </div>
    <% end %>

  
      <%= yield %>

    <footer class="branding">
      <small>&copy; 2018 <strong>DietTrackerSinatraApp</strong></small>
      <a href="/users/<%=current_user.id%>/edit"> <button> Edit Account Info </button> </a>
      <a href="/users/$%=current_user.id%>/delete"> <button> Delete Account </button> </a>
    </footer>
  </div>

  <script src="http://ajax.googleapis.com/ajax/libs/jquery/1/jquery.min.js"></script>
</body>
